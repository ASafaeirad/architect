# tmux display things in 256 colors
set -g default-terminal "tmux-256color-italic"
set -as terminal-overrides ',xterm*:Tc:sitm=\E[3m'

set -g history-limit 10000

# automatically renumber tmux windows
set -g renumber-windows on

# Send prefix
unbind C-a
set -g prefix C-a
bind C-a send-prefix

# Activity Monitoring
setw -g monitor-activity off
set -g visual-activity off

# Rather than constraining window size to the maximum size of any client
# connected to the *session*, constrain window size to the maximum size of any
# client connected to *that window*. Much more reasonable.
setw -g aggressive-resize on

# make delay shorter
set -sg escape-time 0

# tile all windows
unbind =
bind = select-layout tiled

set-option -g set-titles on
set-option -g set-titles-string "#T - #W"
# set-window-option -g automatic-rename on

# Mose mode
set -g mouse on

######################
#### Key Bindings ####
######################

# Config reload
bind r source-file ~/.tmux.conf \; display-message "Configuration Reloaded"

# split window and fix path for tmux 1.9
bind - split-window -v -c "#{pane_current_path}"
bind | split-window -h -c "#{pane_current_path}"

# pane movement shortcuts
bind -n M-h select-pane -L
bind -n M-j select-pane -D
bind -n M-k select-pane -U
bind -n M-l select-pane -R

bind -n M-Left select-pane -L
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D
bind -n M-Right select-pane -R

bind Left select-pane -L
bind Down select-pane -D
bind Up select-pane -U
bind Right select-pane -R

bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

bind -r C-h select-window -t :-
bind -r C-l select-window -t :+

# Resize pane shortcuts
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# set vi mode for copy mode
setw -g mode-keys vi

# more settings to make copy-mode more vim-like
unbind [
bind Escape copy-mode
unbind p
bind p paste-buffer
bind -Tcopy-mode-vi 'v' send -X begin-selection
bind -Tcopy-mode-vi 'y' send -X copy-pipe-and-cancel "tmux save-buffer - | reattach-to-user-namespace pbcopy"

######################
#### Color Schema ####
######################

mainbg=colour0
darkbg=colour236
warn=colour1
main=colour4
hilight=colour3
disable=colour240

set -g status-left 32
set -g status-right 150
set -g status-interval 5

#  modes
setw -g clock-mode-colour $main
setw -g mode-attr bold
setw -g mode-fg colour1
setw -g mode-bg $main

# panes
set -g pane-border-bg $mainbg
set -g pane-border-fg $disable
set -g pane-active-border-bg $mainbg
set -g pane-active-border-fg $hilight

# statusbar
set -g status-position bottom
set -g status-justify left
set -g status-bg $main
set -g status-fg $darkbg
set -g status-attr bold
set -g status-left ''
set -g status-right-bg $main
set -g status-right ' %H:%M '
set -g status-right-length 50
set -g status-left-length 20

setw -g window-status-current-fg $darkbg
setw -g window-status-current-bg $hilight
setw -g window-status-current-attr bold
setw -g window-status-current-format ' #I:#W#F '

setw -g window-status-fg $darkbg
setw -g window-status-bg $main
setw -g window-status-attr none
setw -g window-status-format ' #I:#W#F '

setw -g window-status-bell-attr bold
setw -g window-status-bell-fg $darkbg
setw -g window-status-bell-bg $warn

# messages
set -g message-attr bold
set -g message-fg $darkbg
set -g message-bg $hilight
